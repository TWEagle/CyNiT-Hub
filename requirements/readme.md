# CyNiT-Hub requirements

Install all:
    pip install -r requirements/all.in

Install only hub:
    pip install -r requirements/base.in

Install only tray:
    pip install -r requirements/tray.in

Notes:
- pdfkit requires wkhtmltopdf installed separately and available in PATH.
  https://wkhtmltopdf.org/downloads.html
